const Company = require('../modals/company');
const Talent = require('../modals/talent');

const normalLike = async (req, res) => {
  try {
    const {};
  } catch (error) {
    res.status(400).json({ error: error.message });
  }
};
